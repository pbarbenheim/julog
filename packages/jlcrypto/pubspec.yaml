name: jlcrypto
description: RSA key-pair generation, signing, and AES encryption primitives for JuLog.
version: 1.0.0
publish_to: 'none'

resolution: workspace

environment:
  sdk: ^3.10.4

# Add regular dependencies here.
dependencies:
  meta: ^1.3.0
  pointycastle: ^4.0.0
  uuid: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
