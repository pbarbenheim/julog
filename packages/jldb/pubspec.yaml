name: jldb
description: SQLite persistence layer and domain models for JuLog.
version: 1.0.0-experimental
publish_to: 'none'

resolution: workspace

environment:
  sdk: ^3.10.4

dependencies:
  freezed_annotation: ^3.1.0
  meta: ^1.17.0
  mutex: ^3.1.0
  sqlite3: ^2.9.0
  uuid: ^4.5.2

dev_dependencies:
  build_runner: ^2.10.4
  build_test: ^3.5.4
  freezed: ^3.2.4
  lints: ^6.0.0
  test: ^1.25.6
